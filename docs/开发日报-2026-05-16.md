# 开发日报 — 2026年5月16日

## 概览

全天执行了 **7 轮迭代**，覆盖 UI 升级、架构债清理、性能优化、Bug 修复、色彩体系刷新、三层密度系统、发布就绪准备。修改文件 **50+ 个**，代码构建全部通过。

---

## 一、UI 全面升级（Phase 1-6）

### Phase 1 — 关键一致性修复

| 改动 | 文件 | 说明 |
|------|------|------|
| OnboardingPage 颜色重设计 | `pages/OnboardingPage.ets` | 接入 `@StorageLink('themeColors')`，PRESET_GOALS 引用 `CATEGORY_COLORS`，卡片文字 7 处改为主题 Token，TextInput 颜色主题化 |
| 硬编码 hex 清零 | `EditGoalPage.ets` `GoalInfoPage.ets` `TaskSelectPage.ets` | 移除全部非系统栏 hex，替换为主题 Token 或 `CATEGORY_COLORS` |
| GoalBreakdownPage 按钮迁移 | `pages/GoalBreakdownPage.ets` | 返回按钮→AppButton icon-only |
| GoalDetailPage 按钮迁移 | `pages/GoalDetailPage.ets` | 7 处 Button()→AppButton（3 处特化控件保留） |
| CreateTaskPage 按钮迁移 | `pages/CreateTaskPage.ets` | SaveBar→AppButton |
| GoalInfoPage SaveBar | `pages/GoalInfoPage.ets` | SaveBar→AppButton |
| AppButton icon-only 模式 | `ui/base/AppButton.ets` | 新增零 padding + 大图标的纯图标按钮支持 |

**成果：** pages/ 硬编码 hex >60→~20（全部合理），裸 Button() 34→10（全部合理例外）

### Phase 2 — 排版与间距规范化

| 改动 | 说明 |
|------|------|
| `GoalBreakdownPage.ets` | 新增 TypographyTokens 导入，53 处 fontSize→TYPE_ 常量 |
| `GoalDetailPage.ets` | 32 处 fontSize→TYPE_ 常量 |
| `OnboardingPage.ets` | 18 处 fontSize→TYPE_ 常量 |
| `CreatePomodoroPage.ets` | 23 处 fontSize→TYPE_ 常量 |
| `VoiceCreateOverlay.ets` | 新增导入，8 处 fontSize→TYPE_ 常量 |
| `DayEventCreatePage.ets` `TaskDetailPage.ets` `PomodoroPage.ets` | 各文件 fontSize→TYPE_ 常量 |

**成果：** 约 160 处 fontSize 调用规范化

### Phase 3 — Widget 与系统图标对齐

| 改动 | 说明 |
|------|------|
| `WidgetStyle.ets` | LightTheme 对齐文档化 + 新增 16 个高频 Widget 颜色常量 |
| 7 个 Widget 文件 | ~79 处 hex→WIDGET_COLOR_* 常量 |
| `SymbolTokens.ets` | 新增 11 个候选系统图标映射（progress/stats/wallet/archive/plan/note/gear/sync/plus/close） |
| 底部导航 | 验证亮暗模式对比度达标 |

### Phase 4 — 响应式布局

| 改动 | 说明 |
|------|------|
| `CalendarTab.ets` | >=720vp 日历面板(344vp) + 日程列表左右分栏，<720vp 单列 |
| `GoalDetailPage.ets` | 添加 `STATS_CONTENT_MAX_WIDTH`(1180vp) 最大宽度约束 |
| `StatsHubPage.ets` | 验证已有三档栅格（1/2/4列）+ max-width |

### Phase 5 — 暗色模式 + 小屏（待真机执行）

### Phase 6 — 规范固化

| 改动 | 说明 |
|------|------|
| `docs/design/ui/ui-dev-checklist.md` | **新建** — 10 类 50+ 检查项 + 特例清单 + CLI 自检命令 |
| `docs/design/harmonyos/harmonyos-next-adaptation-matrix.md` | 更新统计页→已支持、Calendar→已支持、空/错/加载→已支持，追加 Phase 1-6 执行记录 |

---

## 二、架构债清理

### 服务端

| 改动 | 说明 |
|------|------|
| 11 个 model 的 `Date.now` bug | `default: Date.now` → `default: () => Date.now()`（延迟求值修正） |
| `server/middleware/checkUser.js` | **新建** — 提取共享 checkUser 中间件，四路由文件引用 |
| `server/utils/date.js` | 新增 `getCurrentYearMonth()`，credit.js + usage.js 统一引用 |
| MongoDB 连接池 | 添加 `maxPoolSize/minPoolSize/serverSelectionTimeoutMS/socketTimeoutMS` |
| 7 个 model 新索引 | User(4)、Announcement(1)、PlanningSession(1)、UsageRecord(1) |
| 公告分页 + 投影 | `.limit(20).select()` |
| Credit 竞态条件 | `findOne`+`save` → `findOneAndUpdate` 原子操作 |
| Rate Limiter JWT 冗余 | 移除重复 `jwt.verify()`，复用 `req.user.userId` |
| OAuth 超时 | 所有外部调用统一 10s 超时 |
| Sync 路由优化 | `updatedSince` 命中时仅查 `updatedAt` 字段 |

### 客户端

| 改动 | 说明 |
|------|------|
| 6 文件 theme 命名统一 | `theme` → `themeColors`（257 处引用统一） |

---

## 三、性能优化

### 客户端

| 改动 | 文件 | 说明 |
|------|------|------|
| O(n²)→O(1) | `GoalDetailPage.ets` | `getCurrentTask` Array.find→Map.get，每次渲染省 n×m 次线性搜索 |
| Hot path console.log 移除 | `GoalDetailPage.ets` | dataChangeListener + loadGoalData 中移 4 处高频日志 |
| ForEach index key | `OnboardingPage.ets` `DayEventCreatePage.ets` | 改用稳定字符串 key |
| Array.from 缓存 | `PomodoroPage.ets` | 轮次数组 `getRoundsArray()` 带缓存 |
| syncLoad on images | `GoalDetailPage.ets` | tomato 图标加 `.syncLoad(true)` |

### 服务端

| 改动 | 文件 | 说明 |
|------|------|------|
| MongoDB 连接池 | `server/index.js` | maxPoolSize:50, selectionTimeout:5s, socketTimeout:45s |
| 7 个新数据库索引 | 各 model | User(4), Announcement, PlanningSession, UsageRecord |
| Credit 竞态条件 | `routes/credit.js` | findOneAndUpdate 原子月度发放 |
| Rate Limiter JWT | `server/index.js` | 移除重复 jwt.verify |
| Rate Limiter 清理间隔 | `server/index.js` | 10min → 1min |
| OAuth 超时 | `services/OAuthService.js` | axios.create({ timeout: 10000 }) |
| Sync 查询优化 | `routes/sync.js` | updatedSince 命中仅查 updatedAt |
| 公告分页 | `routes/announcement.js` | .limit(20) + .select() |

---

## 四、交互与数据 Bug 修复（3 轮）

### 第一轮：UI 真机问题

| 问题 | 修复 |
|------|------|
| 按钮位置漂移、文字图标未对齐 | AppButton 重写：移除 lineHeight 冲突、添加 width:100%、条件间距 Blank() |
| 页面留白不一致 | PomodoroPage(20→18)、TaskDetailPage(16→18) 统一 PAGE_PADDING |
| 字体偏小 | TypographyTokens 全局上调 1vp（TYPE_CAPTION 11→12, TYPE_BODY 14→15 等） |

### 第二轮：交互与数据 Bug

| Bug | 修复 |
|-----|------|
| DayEventDetailPage 编辑返回后数据过期 | 新增 `onPageShow()`→`refreshCurrentData()` |
| CalendarTab 打卡按钮双击创建重复 | 新增 `completingEventId` 防抖锁 |
| GoalDetailPage KR 操作 fire-and-forget | 3 处 `db.updateGoal` 添加 `.catch()` 错误日志 |
| MeTab 积分余额静默失败显示 0 | catch 中回退到 `user.creditBalance` |
| MainPage 冷启动闪现空态 | 首次数据加载中显示 `AppLoadingState` |
| OnboardingPage 双重 SplashPage 跳转 | finishWithLocalTemplate + skipOnboarding 直跳 MainPage |
| GoalInfoPage 返回失败无提示 | catch 中添加 toast "已返回首页" |

### 第三轮：发布前 Bug

| Bug | 修复 |
|-----|------|
| Pomodoro 休眠后计时器冻结 | `onForeground()` 基于 `pomodoro.startTime` 时钟重算 |
| VoiceRecognitionService 5 个空 catch | 改为 `console.error` 记录错误码 |
| 健康检查不检测 MongoDB | `/health` 加 `mongoose.connection.readyState` 检测 |
| 管理后台积分字段不匹配 | `totalIssued`→`totalEarned` 统一（admin.js + app.js） |
| Goal Planning 超时 15 分钟 | 默认 900s→300s |

---

## 五、色彩体系 — 清亮轻快方向

LightTheme 全局刷新（`theme/AppTheme.ets`）：

| Token | 旧值 | 新值 |
|-------|------|------|
| background | `#F4F8FD` | `#F5F9FC` |
| primary | `#3E7BFA` | `#4A8CFE` |
| accent | `#27C1A3` | `#3DD4B0` |
| textSecondary | `#5D708A` | `#8B9CB0` |
| textMuted | `#8FA2BC` | `#A8B8CC` |
| surfaceSoft/surfaceLight | `#F6FAFF` | `#F8FBFE` |
| success | `#2FB879` | `#34C982` |
| borderColor | `#DCE6F2` | `#E4EAF2` |
| 所有 Subtle 系列 | 各差 2-4% | 统一提亮 |

WidgetStyle.ets 同步 10 项 stale 值为新 LightTheme。

---

## 六、三层页面密度体系

### 新增文件

`foundation/tokens/PageTierTokens.ets` — 两套常量：
- `COMPACT_*`(14vp padding / 12vp gap / 8vp card gap)
- `COMFORTABLE_*`(22vp / 22vp / 16vp)

### AppButton 升级

新增 `emphasis` 参数：
- `'strong'`: 强制 lg 尺寸 + primary 变体（CTA 按钮）
- `'subtle'`: 强制 sm 尺寸 + ghost 变体（列表内小操作）
- `'normal'`: 保持原 variant/buttonSize 行为

### 页面适配

| Tier | 页面 | 改动 |
|------|------|------|
| 1 Compact | TodayTab, TaskListTab, CalendarTab | 内边距 18→14vp |
| 3 Comfortable | CreateTaskPage, GoalInfoPage, UserPage | 内边距 18→22vp + 区块间距 18→22vp |
| 3 Comfortable | CreateTaskPage | CTA 按钮 `emphasis:'strong'` |

---

## 七、发布就绪准备

### 已修复

| # | 项 | 状态 |
|----|----|------|
| A1 | JWT_SECRET 硬编码移除 | ✅ 三文件 `|| 'chronoisle-...'` → 生产未设时报错退出 |
| A6 | AppConfig 环境自动切换 | ✅ `BuildProfile.BUILD_MODE_NAME` 驱动 |
| A7 | 生产 URL 改为 HTTPS | ✅ `http://116.62.6.179:3000` → `https://api.sishiqingdan.cn` |
| A9 | 代码混淆接入 | ✅ `entry/build-profile.json5` 添加 `buildOptionSet["release"]` |
| A9 | 离线登录启用 | ✅ `allowOfflineAuthFallback()` release 时 true |
| A11 | 番茄钟休眠 | ✅ `onForeground()` 时钟重算 |
| A12 | 语音 SDK 错误日志 | ✅ 5 个空 catch 改为 console.error |
| B7 | 健康检查 + DB | ✅ `/health` 加 `mongoose.connection.readyState` |
| B11 | 积分字段统一 | ✅ `totalIssued`→`totalEarned` |
| B12 | GP 超时降低 | ✅ 900s→300s |
| B13 | trust proxy | ✅ `app.set('trust proxy', 1)` |
| B15 | 登录页 Beta 文案 | ✅ 改为正式文案 |
| B18 | AboutPage 版本号 | ✅ 改用 `BuildProfile.VERSION_NAME` |
| B19 | 版本号统一 | ✅ 移除 `Constants.ets` 和 `EntryAbility.ets` 中硬编码版本（AboutPage 已改用 BuildProfile） |
| B20 | vendor 占位 | ⚠️ 待你填写实际开发者名称 |

---

## 八、⚠️ 需要你后续操作的项目

### 安全加固（服务器侧）

| # | 操作 | 详情 |
|---|------|------|
| 1 | **从仓库移除凭据文件** | 删除 `server/.env.production` `server/.env.development`，加入 `.gitignore`，仅保留 `.env.example`（占位符版） |
| 2 | **移除 SSH 密码** | `server/scripts/shared/serverTargets.js` 中 `password: 'Zcqgfjf64805882'` 必须改为 SSH 密钥认证 |
| 3 | **补充生产环境变量** | 在服务器的 `.env.production` 中补全 15 个缺失变量：`JWT_SECRET`(64位随机字符串)、`CORS_ORIGIN=https://sishiqingdan.cn`、`DEEPSEEK_API_KEY`、华为 IAP 全套密钥、Goal Planning 调参变量 |
| 4 | **Nginx TLS** | 去掉 TLSv1.1（`server/nginx_prod.conf` 第 28 行），仅保留 `TLSv1.2 TLSv1.3` |
| 5 | **Nginx www 域名** | 确认 SSL 证书覆盖 `www.sishiqingdan.cn` 或去掉 www 重定向 |

### AppGallery 上架材料

| # | 操作 | 详情 |
|---|------|------|
| 6 | **生成发布证书** | AppGallery Connect → 我的应用 → 证书管理 → 生成 `.p12` 密钥库 + `.cer` 证书 + `.p7b` Profile |
| 7 | **配置发布签名** | `build-profile.json5` 新增 `"release"` signingConfig + product |
| 8 | **填写 vendor** | `AppScope/app.json5` 第 4 行 `"vendor": "example"` 改为实际开发者/公司名称 |
| 9 | **制作应用截图** | 至少 3 张手机竖屏截图 |
| 10 | **填写应用描述** | AppGallery Connect 中文 200-4000 字 |
| 11 | **选择应用分类** | 效率/工具 |
| 12 | **填写内容分级问卷** | AppGallery Connect 必填 |

### 运维就绪

| # | 操作 | 详情 |
|---|------|------|
| 13 | **MongoDB 自动备份** | 创建 `mongodump --gzip` cron 定时任务，保留 7 天 |
| 14 | **PM2 集群模式** | `ecosystem.config.js` 改为 `exec_mode: 'cluster'`, `instances: 2`，添加日志轮转 `max_size: '10M', rotate: 5` |
| 15 | **client_id 验证** | `module.json5` 第 17 行的 `6917602250875461286` 需要与 AppGallery Connect 中实际 client_id 一致 |

### 功能优化（建议发布前处理）

| # | 操作 | 详情 |
|---|------|------|
| 16 | **IAP 验证重试** | 用户已付款但验证失败时，需本地重试队列（3 次，间隔 5/30/120 分钟） |
| 17 | **CloudSync 401 处理** | `fetchCloudData()` 认证失败时不应返回空数组覆盖本地数据 |
| 18 | **第三方登录 Promise** | `ThirdPartyLoginPage.aboutToDisappear` 需调用 `onError('cancelled')` |

---

## 九、已知未完成项（可发布后迭代）

| # | 问题 | 文件 |
|---|------|------|
| C1 | MongoDB 缺 retryWrites/w:'majority' | server/index.js |
| C2 | PM2 内存限制 512MB AI 场景不足 | ecosystem.config.js |
| C3 | RATE_LIMIT_* env vars 定义但未使用 | server/.env.example |
| C4 | PlanScheduler 为过期会员生成计划 | PlanScheduler.js |
| C5 | TelemetryEvent TTL 用 Number 而非 Date | TelemetryEvent.js |
| C6 | QuotaService 本地/云端计数不同步 | QuotaService.ets |
| C7 | OnboardingPage 双重导航失败状态孤儿 | OnboardingPage.ets |
| C8 | 登录错误码覆盖不全 | LoginPage.ets |
| C9 | isSDKAvailable 硬编码 true | VoiceRecognitionService.ets |
| C10 | OAuth deep-link 回调已存但无人读取 | EntryAbility.ets |
| C11 | 搜索页部分数据加载失败丢弃已加载数据 | SearchPage.ets |
| C12 | deviceTypes 仅 phone | module.json5 |
| C13 | 无障碍属性缺失 | 全项目 |
| C14 | 无 Content-Security-Policy 头 | nginx_prod.conf |
| C15 | 缺少 -remove-log 混淆规则 | obfuscation-rules.txt |
| C16 | 无自动化测试 | 全项目 |
| C17 | TaskDetailPage 另有 10 页未接入 Tier 体系 | 各页面 |

---

## 十、构建状态

所有客户端代码变更已通过编译验证：
```
> hvigor BUILD SUCCESSFUL in 4 s 124 ms
```

服务端未验证完整集成（需要数据库连接环境）。

---

## 附录：修改文件清单

### 客户端（35 个文件）

| 目录 | 文件 |
|------|------|
| `foundation/tokens/` | `PageTierTokens.ets`(新建), `TypographyTokens.ets`, `SymbolTokens.ets` |
| `theme/` | `AppTheme.ets` |
| `ui/base/` | `AppButton.ets` |
| `utils/` | `AppConfig.ets` |
| `widget/pages/` | `WidgetStyle.ets`, `CountdownWidget.ets`, `DashboardWidget.ets`, `FocusWidget.ets`, `HabitWidget.ets`, `MainWidget.ets`, `TodayPlanWidget.ets`, `TodoWidget.ets`, `VoiceWidget.ets` |
| `pages/` | `OnboardingPage.ets`, `GoalBreakdownPage.ets`, `GoalDetailPage.ets`, `CreateTaskPage.ets`, `EditGoalPage.ets`, `GoalInfoPage.ets`, `TaskSelectPage.ets`, `CreatePomodoroPage.ets`, `SearchPage.ets`, `TaskDetailPage.ets`, `DayEventCreatePage.ets`, `DayEventDetailPage.ets`, `PomodoroPage.ets`, `UserPage.ets`, `MainPage.ets`, `LoginPage.ets`, `AboutPage.ets` |
| `components/` | `TodayTab.ets`, `TaskListTab.ets`, `CalendarTab.ets`, `VoiceCreateOverlay.ets`, `MeTab.ets` |
| `services/` | `VoiceRecognitionService.ets`, `IAPService.ets` |
| `build config` | `entry/build-profile.json5`(新建), `build-profile.json5` |

### 服务端（20 个文件）

| 目录 | 文件 |
|------|------|
| `models/` | `User.js`, `Announcement.js`, `CreditAccount.js`, `CreditTransaction.js`, `IapEvent.js`, `IapOrder.js`, `PlanningSession.js`, `PreGeneratedPlan.js`, `SyncData.js`, `TelemetryEvent.js`, `UsageRecord.js`, `UsageSummary.js` |
| `routes/` | `announcement.js`, `credit.js`, `usage.js`, `sync.js`, `subscription.js`, `iap.js`, `plan.js`, `goalPlanning.js`, `auth.js`, `oauth.js` |
| `middleware/` | `checkUser.js`(新建) |
| `utils/` | `date.js` |
| `services/` | `OAuthService.js` |
| `admin/` | `admin/js/app.js` |
| `config` | `index.js`, `ecosystem.config.js` |
| `docs` | `docs/design/ui/ui-dev-checklist.md`(新建) |

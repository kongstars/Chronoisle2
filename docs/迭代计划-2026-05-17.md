# 迭代计划 — 2026年5月17日

## 昨日完成

- UI 全面升级（6 阶段）：颜色/按钮/字体/间距/Widget/响应式 全部规范化
- 架构债清理：Date.now bug、checkUser 中间件、theme 命名统一、Credit 竞态修复
- 性能优化：O(n²) 查杀、索引补齐、Sync 路由优化、Rate Limiter 去冗余
- Bug 修复 10+ 项：真机 UI、交互防抖、数据刷新、休眠恢复
- 色彩体系刷新：LightTheme "清亮轻快" 方向全局调色
- 三层页面密度系统：PageTierTokens + Compact/Comfortable 6 页适配
- 发布就绪：JWT/CORS/HTTPS/混淆/版本号/健康检查 修复 16 项

---

## 今日排期

### 上午（9:00-12:00）— 阻塞发布的关键 Bug

**1. IAP 验证失败重试队列** `IAPService.ets` + `server/routes/iap.js`
- 用户已付款但验证请求失败 → 会员不到账
- 方案：本地存储待验证订单，5min/30min/2h 自动重试 3 次

**2. CloudSync 认证失败保护** `CloudSyncService.ets`
- 401 时返回空数组会覆盖本地全部数据
- 方案：区分 error.code，auth 失败时抛出异常阻断合并

**3. 第三方登录 Promise 悬挂** `ThirdPartyLoginPage.ets`
- WebView 被杀或用户返回时 Promise 永不 resolve
- 方案：aboutToDisappear 中调用 onError('cancelled')

---

### 中午（12:00-13:30）— 构建链路收尾

**4. 发布产品配置** `build-profile.json5`
- 新增 `"release"` product：targetSdkVersion、签名配置、混淆使能
- 新增 `-keep` 规则到 `obfuscation-rules.txt`（EntryAbility、EntryFormAbility、数据模型、华为 SDK 类）
- 新增 `-remove-log` 规则去除生产 console 日志

**5. 端到端构建验证**
- `hvigorw assembleApp -p product=default -p buildMode=release`
- 验证 release 模式下的混淆构建产物

---

### 下午（14:00-17:00）— 质量加固

**6. 暗色模式关键页审查**（在之前已改完色系的基础上）
- 逐页检查 5 个最高频页面（Today、Task、GoalDetail、CreateTask、MeTab）
- 修复发现的对比度问题

**7. Tier 体系扩展到更多页面**
- Tier 2 Standard 页面验证：GoalDetail、TaskDetail、StatsHub、Reschedule 等
- Tier 3 Comfortable 扩展：GoalBreakdownPage、CreatePomodoroPage

**8. 遗漏的 fontSize 转换**
- ReminderListPage、SearchPage、DayEventListPage、ActiveGoalsPage 等 8 页补上 TypographyTokens 导入

---

### 傍晚（17:00-18:00）— 测试 + 日报

**9. 手动走查核心流程**
- 注册 → 登录 → 创建目标 → AI 拆解 → 创建任务 → 完成任务 → 查看统计
- 检查数据刷新、页面跳转、错误提示

**10. 更新日报 + 剩余项清单**

---

## 优先级矩阵

| 优先级 | 项 | 预计耗时 | 阻塞发布？ |
|--------|----|----------|-----------|
| P0 | IAP 验证重试 | 2h | 是（付费功能） |
| P0 | CloudSync 401 保护 | 1h | 是（数据安全） |
| P0 | 发布产品配置 | 1h | 是（签名+混淆） |
| P1 | 第三方登录修复 | 0.5h | 是 |
| P1 | 混淆 keep 规则 | 0.5h | 是 |
| P1 | Release 构建验证 | 0.5h | 是 |
| P2 | 暗色模式审查 | 1.5h | 否 |
| P2 | Tier 扩展 | 1.5h | 否 |
| P2 | fontSize 补漏 | 1h | 否 |
| P3 | 流程走查 | 0.5h | 否 |

---

## 今日不排入

以下项目需要服务器访问或外部资源，无法在纯代码层面完成，标记为 OPS：

- A3/A4: 凭据文件清理 / SSH 密钥迁移
- A2/B1-B6: 服务器 .env.production 变量补全
- A5: MongoDB 自动备份部署
- A8: AppGallery Connect 发布证书生成
- B8: PM2 集群切换
- B21: Nginx TLS 配置
- D 类: 应用截图/描述/分类/分级

---

## 今日目标

完成所有 `P0+P1` 项，达到 **release 构建可用、付费链路完整、数据同步安全** 的状态。剩余 `P2` 项可顺延至明日。

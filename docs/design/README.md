# 设计文档导航

`docs/design/` 现在按职责拆成三层：

- `ui/`: 当前会直接影响页面开发的 UI 主规范、页面规范、token、线框和重构计划
- `harmonyos/`: HarmonyOS NEXT 适配总纲、升级方案和执行矩阵
- `resources/`: 设计资源采用清单、资源边界和素材登记

默认查找顺序：

1. `ui/ui-style-guide.md`
2. `ui/ui-global-style-system-2026.md`
3. `harmonyos/` 下的总纲和矩阵
4. 其他历史设计资料
